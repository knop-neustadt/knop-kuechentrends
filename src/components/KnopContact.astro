---
import contactPicture from "../assets/images/contactPicture.jpg";
import Image from "astro/components/Image.astro";
---

<section class="formSection">
    <div class="formWrapper">
        <div class="formLeft">
            <div class="contactGreeting">
                <h2>Kontaktieren Sie uns!</h2>
                <p>
                    Wir sind für Sie da, wenn Sie Fragen haben oder eine
                    Beratung brauchen.
                </p>
            </div>
            <form action="https://api.web3forms.com/submit" method="POST">
                <input
                    type="hidden"
                    name="access_key"
                    value="674c350f-81d6-4780-bedd-db6eb6f08a74"
                />
                <div class="formInput">
                    <label for="name">Name:*</label>
                    <input type="text" id="name" name="name" required />
                </div>
                <div class="formInput">
                    <label for="email">E-Mail:*</label>
                    <input type="email" id="email" name="email" required />
                </div>
                <div class="formInput">
                    <label for="number">Telefonnummer:</label>
                    <input
                        type="text"
                        id="number"
                        name="Phone Number"
                        required
                    />
                </div>
                <div class="formInput">
                    <label for="subject">Betreff:*</label>
                    <input type="text" id="subject" name="subject" required />
                </div>
                <div class="formInput">
                    <label for="message">Ihre Nachricht:*</label>
                    <textarea name="message" required></textarea>
                </div>
                <button type="submit">Absenden</button>
            </form>
        </div>
        <Image
            src={contactPicture}
            alt="Bild einer Musterküche von Knop Neustadt"
            width={600}
            widths={[600, 800, 1200]}
            sizes="(max-width: 600px) 600px,(max-width:800px) 800px, (max-width: 1200px) 1200px"
            loading={"lazy"}
        />
    </div>
</section>

<style>
    input,
    textarea,
    button {
        border: 1px solid var(--color-dark);
        padding: 1rem;
    }
    button {
        padding: 0.75rem;
        cursor: pointer;
        transition: all ease-in-out 0.1s;
        width: 100%;
    }
    button:hover {
        background-color: var(--color-dark);
        color: var(--color-light);
    }

    .formInput {
        display: flex;
        flex-direction: column;
        padding-bottom: 1rem;
    }

    textarea {
        height: 100%;
        resize: none;
        min-height: 260px;
    }

    img {
        aspect-ratio: 3/4;
        padding-top: 2rem;
        object-position: top;
    }

    .contactGreeting {
        padding: 0rem 0rem 1rem 0rem;
    }

    @media screen and (min-width: 760px) {
        .formSection {
            min-height: 80vh;
        }

        .formWrapper {
            display: flex;
            justify-content: space-between;
            gap: 2rem;
            height: 100%;
            align-items: center;
        }

        img {
            aspect-ratio: 1/1;
            max-width: 50%;
            height: 100%;
            padding: 0;
            object-position: top;
        }

        form {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .formInput {
            padding: 0;
        }

        .formInput:last-of-type {
            display: flex;
            flex-direction: column;
            flex: 1;
            height: 100%;
        }

        .formLeft {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        textarea {
            flex: 1;
            min-height: auto;
        }
    }

    @media screen and (min-width: 1280px) {

        .formSection{
            height: 100vh;
        }

        .formWrapper {
            gap: var(--padding-desktop);
        }
    }
</style>
