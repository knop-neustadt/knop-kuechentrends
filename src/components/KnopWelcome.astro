<div class="welcomeWrapper">

    <div class="stylediv"></div>
    <div class="welcomeTextWrapper">
        <p class="subHeading">Küchentrends</p>
        <h1>Ihr Partner <span>für die neue Küche</span> in Neustadt!</h1>

        <p class="welcomeParagraph">
            Knop Neustadt schafft Küchen, die leben. Mit handwerklicher
            Präzision und modernem Design entstehen Räume, in denen Qualität
            spürbar wird.
        </p>

        <div class="welcomeButtonsWrapper">
            <a href="/galerie">Galerie</a>
            <a href="#kontakt">Kontakt</a>
        </div>
    </div>
</div>
<style>
    span {
        display: block;
    }
    .welcomeWrapper {
        padding: 1rem;
        display: grid;
        min-height: 100svh;
        background-image: url(/src/assets/images/welcomePicture.webp);
        background-position: center;
        background-size: cover;
    }

    .stylediv {
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 60%;
        background: #000;
        background: #19181f;
        background: linear-gradient(
            0deg,
            rgba(25, 24, 31, 1) 30%,
            rgba(0, 0, 0, 0) 100%
        );
    }

    .welcomeTextWrapper {
        color: var(--color-light);
        align-self: flex-end;
        z-index: 1;
    }

    h1 {
        font-weight: normal;
        font-size: 3.5rem;
        font-weight: 600;
        line-height: 3rem;
        margin-bottom: 5rem;
        text-transform: capitalize;
    }

    .welcomeButtonsWrapper {
        display: flex;
        align-items: center;
        font-weight: 600;
        transform: translate(-0.5rem);
        margin-top: 1rem;
        a {
            color: var(--color-light);
            cursor: pointer;
            margin-right: 1rem;
            font-size: 1.25rem;
            padding: 0.5rem;
            transition: all ease-in-out 0.1s;
            text-decoration: underline;

            &:hover {
                background-color: var(--color-light);
                color: var(--color-dark);
            }
        }
    }

    @media screen and (min-width: 1024px) {
        .welcomeWrapper {
            padding: 2.5rem;
            background-image: url(/src/assets/images/welcomePictureDesktop.webp);
        }

        .welcomeTextWrapper {
            align-self: center;
        }

        p.welcomeParagraph {
            max-width: 600px;
            font-size: 1.5rem;
        }

        h1 {
            line-height: 6.75rem;
            font-size: 8rem;
            margin-bottom: 7.5rem;
        }

        .stylediv {
            height: 100%;
            width: 50%;
            background: #19181f;
            background: linear-gradient(
                90deg,
                rgba(25, 24, 31, 1) 30%,
                rgba(0, 0, 0, 0) 100%
            );
        }
    }
</style>
